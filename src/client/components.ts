import { Collapse, Command, Alert, About, Dialog, SnippetInfo, Profile  } from './app/components';
import { Editor, EditorMode, GalleryList, Import, ViewMode, ViewModeError } from './app/containers';

export const COMPONENT_DECLARATIONS = [
    GalleryList,
    Editor,
    EditorMode,
    Import,
    ViewMode,
    ViewModeError,
    Collapse,
    Alert,
    Profile,
    Dialog,
    SnippetInfo,
    About,
    Command,
];
