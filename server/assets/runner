<!DOCTYPE html>
<html>

<head>
    <base href="/">
    <title>{{snippet.name}}</title>

    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="author" content="{{snippet.author}}" />

    <link rel="stylesheet" href="{{snippet.origin}}/runner.bundle.css" />
    <link rel="stylesheet" href="https://static2.sharepointonline.com/files/fabric/office-ui-fabric-js/1.2.0/css/fabric.min.css">

    {{#if snippet.officeJS}}
        <script src="{{snippet.officeJS}}"></script>
    {{/if}}
</head>

<body class="{{snippet.host}}">
    <header id="header" class="command__bar">
        {{#if includeBackButton}}
        <command>
            <a class="command__icon ms-u-slideLeftIn10" href="{{{returnUrl}}}">
                <i class="ms-Icon ms-Icon--Back"></i>
            </a>
        </command>
        {{/if}}
        <command>
            <a class="command__icon command__center ms-u-slideLeftIn10" href="{{{refreshUrl}}}">
                <i class="ms-Icon ms-Icon--Refresh"></i>
                <span class="ms-font-m ms-u-slideLeftIn10">{{snippet.name}}</span>
            </a>
        </command>
    </header>

    <app id="progress">
		<div class="ms-progress-component">
			<main class="ms-progress-component__main">
				<img class="ms-progress-component__logo" src="{{snippet.origin}}/assets/images/icon-large.png">
				<h1 class="ms-progress-component__title ms-font-xxl">{{snippet.name}}</h1>
				<p class="ms-progress-component__sub-title ms-font-m">Compiling...</p>
			</main>
			<footer class="ms-progress-component__footer">
				<div class="ms-progress">
					<div class="cs-loader">
						<div class="cs-loader-inner">
							<label>	●</label>
							<label>	●</label>
							<label>	●</label>
							<label>	●</label>
							<label>	●</label>
						</div>
					</div>
				</div>
			</footer>
		</div>
	</app>

    <pre id="snippet-code-content" style="display:none">{{snippetContent}}</pre>
    <iframe id="snippet-container" src="about:blank"></iframe>

    <script src="{{snippet.origin}}/polyfills.bundle.js"></script>
    <script src="{{snippet.origin}}/vendor.bundle.js"></script>
    <script src="{{snippet.origin}}/runner.bundle.js"></script>
    <script type="text/javascript">
        window.initializeRunner({
            origin: '{{{snippet.origin}}}',
            officeJS: '{{{snippet.officeJS}}}'
        });
    </script>
</body>

</html>